<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>de.gematik.refpopp</groupId>
        <artifactId>popp-example-impl-global</artifactId>
        <version>2.7.0</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>vsdm2-client</artifactId>
    <description>VSDM 2.0 Client Application</description>

    <name>vsdm2-client</name>

    <properties>
        <docker.image.name>${docker.registry.gematik}/popp/vsdm2-client</docker.image.name>
        <commit_hash>undefined</commit_hash>
        <skip.dockerbuild>false</skip.dockerbuild>
        <bouncycastle.version>1.84</bouncycastle.version>
        <api-telematik.version>2026-05-18</api-telematik.version>
        <cxf.version>4.1.3</cxf.version>
        <hapi-fhir.version>6.8.0</hapi-fhir.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web-services</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>de.gematik.zeta</groupId>
            <artifactId>zeta-sdk-jvm</artifactId>
            <version>${version.zeta-sdk-jvm}</version>
        </dependency>
        <!-- Gematik Connector WSDL/XSD stubs (CXF-generated), bundled with their schemas.
             Also supplies the VSDM JAXB types (vsds.vsdservice.v5, fa.vsdm.vsd.v5) used by the
             FHIR -> ReadVSDResponse converter. -->
        <dependency>
            <groupId>de.servicehealth</groupId>
            <artifactId>api-telematik</artifactId>
            <version>${api-telematik.version}</version>
        </dependency>
        <!-- HAPI FHIR R4 — parses the VSDM 2.0 FHIR bundle before mapping it to ReadVSDResponse. -->
        <dependency>
            <groupId>ca.uhn.hapi.fhir</groupId>
            <artifactId>hapi-fhir-base</artifactId>
            <version>${hapi-fhir.version}</version>
        </dependency>
        <dependency>
            <groupId>ca.uhn.hapi.fhir</groupId>
            <artifactId>hapi-fhir-structures-r4</artifactId>
            <version>${hapi-fhir.version}</version>
        </dependency>
        <!-- CXF runtime for JAX-WS SOAP calls -->
        <dependency>
            <groupId>jakarta.xml.ws</groupId>
            <artifactId>jakarta.xml.ws-api</artifactId>
            <version>4.0.2</version>
        </dependency>
        <dependency>
            <groupId>org.apache.cxf</groupId>
            <artifactId>cxf-rt-frontend-jaxws</artifactId>
            <version>${cxf.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.cxf</groupId>
            <artifactId>cxf-rt-transports-http</artifactId>
            <version>${cxf.version}</version>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.bouncycastle</groupId>
            <artifactId>bcprov-jdk18on</artifactId>
            <version>${bouncycastle.version}</version>
        </dependency>
        <dependency>
            <groupId>org.bouncycastle</groupId>
            <artifactId>bctls-jdk18on</artifactId>
            <version>${bouncycastle.version}</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <!-- The Konnektor WSDLs import xmldsig-core-schema.xsd, whose DOCTYPE
                         references an external DTD; the JDK's secure-processing default blocks
                         that and CXF fails to build its ports. -->
                    <jvmArguments>-Djavax.xml.accessExternalDTD=all</jvmArguments>
                </configuration>
            </plugin>
            <plugin>
                <artifactId>maven-resources-plugin</artifactId>
                <executions>
                    <execution>
                        <configuration>
                            <encoding>UTF-8</encoding>
                            <outputDirectory>${basedir}/target/tmpdocker</outputDirectory>
                            <resources>
                                <resource>
                                    <directory>${basedir}/docker</directory>
                                    <includes>
                                        <include>**/*</include>
                                    </includes>
                                </resource>
                                <resource>
                                    <directory>${basedir}/target</directory>
                                    <includes>
                                        <include>vsdm2-client*.jar</include>
                                    </includes>
                                    <excludes>
                                        <exclude>vsdm2-client-*-javadoc.jar</exclude>
                                        <exclude>vsdm2-client-*-sources.jar</exclude>
                                    </excludes>
                                </resource>
                            </resources>
                            <skip>${skip.dockerbuild}</skip>
                        </configuration>
                        <goals>
                            <goal>copy-resources</goal>
                        </goals>
                        <id>copy-docker-resources-jar_and_dockerfile</id>
                        <phase>package</phase>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>io.fabric8</groupId>
                <artifactId>docker-maven-plugin</artifactId>
                <version>${version.docker-maven-plugin}</version>
                <configuration>
                    <images>
                        <image>
                            <build>
                                <args>
                                    <COMMIT_HASH>${commit_hash}</COMMIT_HASH>
                                    <VERSION>${project.version}</VERSION>
                                </args>
                                <dockerFile>${basedir}/target/tmpdocker/Dockerfile</dockerFile>
                            </build>
                            <name>${docker.image.name}:${project.version}</name>
                        </image>
                    </images>
                    <skip>${skip.dockerbuild}</skip>
                    <verbose>true</verbose>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>build</goal>
                        </goals>
                        <id>default</id>
                        <phase>package</phase>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
